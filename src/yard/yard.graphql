
type Query {
    getYard(id: Int): Yard
}

type Mutation {
    addYard(input: YardInput!): Yard
    updateYard(id: Int!, email: String!): Yard
}

type Yard {
    name: String!
    email: String!
}

input YardInput {
    name: String!
    email: String!
}